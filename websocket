Nginx/OpenResty + mm3)：只负责根据 IP 离散算力，保证相同 IP 收敛在同一个 Pod，同时具备平滑的伸缩容能力
在 Nginx 层必须使用 一致性哈希（Consistent Hashing，如 hash $remote_addr consistent;），这样扩容时只会影响几分之一的连接，而不是全量重新洗牌
使用 spring-boot-starter-websocket

server:
  tomcat:
    # 最大的工作线程数，处理控制逻辑
    threads:
      max: 200 
    # 接受连接的队列长度
    accept-count: 1000
    # 最大连接数（必须调大，默认 8192）
    max-connections: 500000 

spring:
  # 限制 WebSocket 的消息缓存大小
  messages:
    websocket:
      max-binary-message-size: 8192 # 8KB
      max-text-message-size: 8192   # 8KB
  #开启虚拟线程
  threads:
    virtual:
      enabled: true

使用ConcurrentHashMap保存每个用户的websocket连接集合ConcurrentHashMap<Long, List<RommSession>>;List里面保存的是房间Id和该用户的连接
afterConnectionClosed连接断开事件时移除集合里面对应的Session，如果用户下面的Session集合为空则从Map里面移除该用户map.remove(userId)

所有websocket的消息收到后发送到KafKa，消息体包括，发送人，发送时间，房间号，消息类型，消息内容，分区键是房间号用MurmurHash3进行hash，这样尽量分散
然后每个websocket服务器会是kafka的消费者，并每个websocket服务器消费组不一样，当收到消息时，检查本地房间号里面在线会员有哪些，本地是有在线会员，本地无该房间号会员则不处理消息，
如果有则发送消息给本地有的会员的websocket连接（前提是发送人不是自己，是自己发送的无需再发送给自己）
不要在 Kafka 消费者侧反序列化完整的消息体。将 Kafka 消息设计为两部分：Header (仅包含房间号) 和 Payload (二进制)。
Java 收到字节流后，先用字节码读取房间号，如果本地没有这个房间，直接丢弃该字节流，绝对不要进 JSON 框架反序列化，以此保护 CPU，
kafka报文用Protobuf，利用 Protobuf 的 CodedInputStream，可以像翻书一样直接定位到第 1 个字段（roomId），读取完立刻决定是否丢弃，后面的 payload 字节数组完全不转为 Java 对象。

针对每个websocket连接要进行限流，使用 Session 的 UserProperties，限制消息发送频率，可以配置，默认间隔时间不能小于900毫秒
Spring Boot 处理 WebSocket 握手时（使用 HandshakeInterceptor），可以从请求头中抓取这个 IP，并将其注入到 Session 的属性中，
因为有了 Nginx 的 IP 一致性 Hash 保证，同一个 IP 的连接必然会落到同一个 JVM 实例上，所以直接用本地内存技术（ConcurrentHashMap + AtomicInteger）
就能精准做到单 IP 最多 10（读取配置文件里面的） 个连接，超过10个，发送连接建立过多的消息，不让建立连接。
同时，为了防止某些极端异常情况下连接断开事件丢失导致计数器“只增不减”（从而导致该 IP 永远无法再建连），必须在心跳扫描（时间轮）时，校验如果连接已失效，强制修正该 IP 的计数器。

房间建立事件，发送kafka消息，内容有房间ID，房间名称，房间生效时间，房间结束时间，是否永久有效，房间管理员列表，房间主播列表，自定义MAP，三方自定义字段，所属租户商户号，是否启动(紧急处理生产情况使用，一般都是启用)
房间修改事件，发送kafka消息，内容有房间ID，房间名称，房间生效时间，房间结束时间，是否永久有效，房间管理员列表，房间主播列表，自定义MAP，三方自定义字段，所属租户商户号，是否启动(紧急处理生产情况使用，一般都是启用)
房间实践消费者：从redis检查房间是否存在，不存在则建立相关redis数据结构，并设置过期时间为房间结束时间+随机的2-10分钟，存在则更新相关值；如果是建立房间的消息，
则同时建立房间在线用户ID的Hmap
删除某个房间的redis用UNLINK 防止大key
用户进入房间事件，本地某房间在线用户列表增加该用户
用户离开房间事件，本地某房间在线用户列表移除该用户
用户连接建立事件，本地某房间在线用户列表增加该用户
用户断开连接事件，本地某房间在线用户列表移除该用户

每个websocket建立连接三秒内必须表明身份，15秒内没有表明身份的发送消息，非法请求，然后强制断开连接；表明身份就是发送消息体商户号、用户id、sign，验证通过就好
心跳10秒一次，3次收不到心跳断开连接
服务端本地为每个连接维护一个 lastActiveTime（最后活跃时间戳）使用 时间轮算法（HashedWheelTimer） 或每隔 10 秒启动一个全局定时任务，统一扫描所有连接，发现 System.currentTimeMillis() - lastActiveTime > 30000 的，
直接由服务端主动 session.close()
为了防止惊群，客服端首次心跳必须是3-12秒中的一个随机抖值，不能是10秒；第二个心跳开始是10秒
增加优雅停机处理上面事务
